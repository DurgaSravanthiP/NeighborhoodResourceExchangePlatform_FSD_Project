{
  "name": "neighbourhood-app",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install --include=dev && npm run build",
    "start": "cd backend && node server.js",
    "install-all": "cd backend && npm install && cd ../frontend && npm install"
  }
}
